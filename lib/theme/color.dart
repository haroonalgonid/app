import 'package:flutter/material.dart';

class AppColors {
  // ألوان أساسية
  static const Color blue = Colors.blue;
  static const Color csstomblue = Color(0xFF4C6EF8);
  static const Color primaryColor = Colors.green;
  static const Color orange = Colors.orange;

  // ألوان النصوص
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;

  // ألوان الخلفيات
  static const Color background = Color(0xFFF5F6FA);
  static Color cardBackground = Colors.grey[200]!;
  static const Color grey = Colors.grey;

  // ألوان الأزرار
  static const Color buttonColor = Colors.blueAccent;
  static const Color buttonTextColor = Colors.white;

  // ألوان الأخطاء والنجاح
  static const Color red = Colors.red;
  static const Color successColor = Colors.green;

  // ألوان مخصصة
  static const Color customColor = Color(0xFF1E88E5); // لون أزرق مخصص
}
